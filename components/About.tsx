'use client';
import {useEffect,useRef} from 'react';
import gsap from 'gsap';
import ScrollTrigger from 'gsap/ScrollTrigger';
import SplitType from 'split-type';
export default function About(){const ref=useRef<HTMLElement>(null);useEffect(()=>{gsap.registerPlugin(ScrollTrigger);if(!ref.current)return;const split=new SplitType(ref.current.querySelector('[data-manifesto]') as HTMLElement,{types:'words'});const ctx=gsap.context(()=>{gsap.set(split.words,{color:'rgba(10,10,10,.16)'});gsap.to(split.words,{color:'#0A0A0A',stagger:.045,ease:'none',scrollTrigger:{trigger:ref.current,start:'top 72%',end:'bottom 60%',scrub:1}})},ref);return()=>{ctx.revert();split.revert()}},[]);return <section ref={ref} id="about" className="section-line overflow-hidden py-24 md:py-40"><div className="mx-auto max-w-[1600px] px-5 md:px-8"><div className="grid grid-cols-12 gap-6"><div className="col-span-12 md:col-span-3"><span className="label text-muted">ABOUT / VISION / 03</span></div><div className="col-span-12 md:col-span-9"><p data-manifesto className="display text-[11vw] font-medium md:text-[5.8vw]">We believe the future belongs to brands that behave less like pages and more like living systems — intelligent, responsive, cinematic and unmistakably human.</p><div className="mt-16 grid grid-cols-1 gap-10 border-t border-black/20 pt-8 md:grid-cols-3"><div><span className="label text-muted">PRINCIPLE 01</span><p className="mt-4 max-w-xs text-sm leading-6">Every motion earns its place. Interaction should reveal meaning, not distract from it.</p></div><div><span className="label text-muted">PRINCIPLE 02</span><p className="mt-4 max-w-xs text-sm leading-6">Technology disappears into the experience. The user feels intelligence before they notice it.</p></div><div><span className="label text-muted">PRINCIPLE 03</span><p className="mt-4 max-w-xs text-sm leading-6">Typography, image, code and strategy are one system — never separate layers.</p></div></div></div></div></div><div className="mt-28 overflow-hidden border-y border-black/20 py-6"><div className="marquee-track text-[10vw] font-medium uppercase tracking-[-.06em] md:text-[6vw]"><span className="whitespace-nowrap pr-8">ARTIFICIAL INTELLIGENCE — BRAND STRATEGY — DIGITAL PRODUCTS — MOTION SYSTEMS — </span><span className="whitespace-nowrap pr-8">ARTIFICIAL INTELLIGENCE — BRAND STRATEGY — DIGITAL PRODUCTS — MOTION SYSTEMS — </span></div></div></section>