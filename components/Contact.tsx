'use client';
import { useRef } from 'react';
import gsap from 'gsap';
import { ArrowUpRight } from 'lucide-react';

export default function Contact(){const btn=useRef<HTMLAnchorElement>(null);const move=(e:React.MouseEvent<HTMLAnchorElement>)=>{if(!btn.current)return;const r=btn.current.getBoundingClientRect();const x=e.clientX-(r.left+r.width/2);const y=e.clientY-(r.top+r.height/2);gsap.to(btn.current,{x:x*.18,y:y*.18,duration:.45,ease:'power4.out'})};const leave=()=>btn.current&&gsap.to(btn.current,{x:0,y:0,duration:.8,ease:'elastic.out(1,.35)'});return <section id="contact" className="section-line relative min-h-screen overflow-hidden px-5 md:px-8 py-24 flex flex-col items-center justify-center text-center"><div className="absolute left-[15%] top-[20%] h-72 w-72 rounded-full bg-black/10 blur-3xl animate-pulse"/><div className="absolute right-[12%] bottom-[14%] h-80 w-80 rounded-full bg-neutral-400/20 blur-3xl animate-pulse"/><p className="relative z-10 text-[10px] uppercase tracking-[.32em] text-muted mb-8">05 / START SOMETHING</p><h2 className="relative z-10 max-w-[1500px] text-[15vw] md:text-[9vw] leading-[.82] tracking-[-.075em] font-medium">LET&apos;S BUILD<br/>THE FUTURE.</h2><a ref={btn} onMouseMove={move} onMouseLeave={leave} href="mailto:hello@pixzen.ai" className="relative z-10 mt-12 rounded-full bg-foreground text-background px-8 py-4 text-xs font-semibold tracking-[.16em] flex items-center gap-3">START A PROJECT <ArrowUpRight size={16}/></a></section>}