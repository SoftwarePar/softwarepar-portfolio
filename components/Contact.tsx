'use client';
import {MoveUpRight} from 'lucide-react';
import {MouseEvent,useRef} from 'react';
import gsap from 'gsap';

export default function Contact(){const button=useRef<HTMLAnchorElement>(null);const move=(e:MouseEvent<HTMLDivElement>)=>{if(!button.current)return;const rect=button.current.getBoundingClientRect();const x=e.clientX-(rect.left+rect.width/2);const y=e.clientY-(rect.top+rect.height/2);const distance=Math.hypot(x,y);if(distance<180)gsap.to(button.current,{x:x*.18,y:y*.18,duration:.45,ease:'power4.out'})};const reset=()=>{if(button.current)gsap.to(button.current,{x:0,y:0,duration:1.1,ease:'elastic.out(1,.35)'})};return <section id="contact" onMouseMove={move} onMouseLeave={reset} className="section-line relative min-h-screen overflow-hidden bg-black px-5 py-28 text-white md:px-8"><div className="absolute left-[10%] top-[20%] h-[35vw] w-[35vw] rounded-full bg-white/15 blur-3xl animate-pulse"/><div className="absolute bottom-[5%] right-[8%] h-[32vw] w-[32vw] rounded-full bg-[#7d7d7d]/35 blur-3xl animate-pulse [animation-delay:1s]"/><div className="relative z-10 mx-auto flex min-h-[78vh] max-w-[1600px] flex-col items-center justify-center text-center"><p className="label mb-7 text-white/45">LET'S MAKE SOMETHING IMPOSSIBLE TO IGNORE</p><h2 className="max-w-[1500px] text-[16vw] font-medium leading-[.78] tracking-[-.075em] mix-blend-difference md:text-[10vw]">Let&apos;s Build<br/>The Future.</h2><p className="mt-9 max-w-xl text-base leading-7 text-white/55">Strategy, artificial intelligence, digital product and motion brought together as one coherent experience.</p><a ref={button} href="mailto:hello@pixzen.ai" className="mt-12 inline-flex items-center gap-6 rounded-full border border-white/35 bg-white px-7 py-4 text-xs font-medium tracking-[.18em] text-black shadow-[0_15px_70px_rgba(255,255,255,.12)]">START A PROJECT <MoveUpRight size={16}/></a></div></section>}
