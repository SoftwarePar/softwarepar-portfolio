gsap.registerPlugin(ScrollTrigger);const canvas=document.querySelector('#world'),scene=new THREE.Scene(),camera=new THREE.PerspectiveCamera(42,innerWidth/innerHeight,.1,100);camera.position.z=5.8;const renderer=new THREE.WebGLRenderer({canvas,alpha:true,antialias:true});renderer.setPixelRatio(Math.min(devicePixelRatio,1.7));renderer.setSize(innerWidth,innerHeight);const group=new THREE.Group();scene.add(group);const geo=new THREE.IcosahedronGeometry(1.15,4),mat=new THREE.MeshBasicMaterial({color:0xb7ff34,wireframe:true,transparent:true,opacity:.18});const core=new THREE.Mesh(geo,mat);group.add(core);const shell=new THREE.Mesh(new THREE.IcosahedronGeometry(1.35,2),new THREE.MeshBasicMaterial({color:0xe8eee5,wireframe:true,transparent:true,opacity:.055}));group.add(shell);const count=900,pos=new Float32Array(count*3);for(let i=0;i<count;i++){const r=2.2+Math.random()*3.8,a=Math.random()*Math.PI*2,b=Math.acos(2*Math.random()-1);pos[i*3]=r*Math.sin(b)*Math.cos(a);pos[i*3+1]=r*Math.sin(b)*Math.sin(a);pos[i*3+2]=r*Math.cos(b)}const pg=new THREE.BufferGeometry();pg.setAttribute('position',new THREE.BufferAttribute(pos,3));const points=new THREE.Points(pg,new THREE.PointsMaterial({color:0xa9b2a5,size:.009,transparent:true,opacity:.42}));scene.add(points);let mx=0,my=0;addEventListener('pointermove',e=>{mx=(e.clientX/innerWidth-.5);my=(e.clientY/innerHeight-.5)});function render(){requestAnimationFrame(render);core.rotation.x+=.0014;core.rotation.y+=.0022;shell.rotation.y-=.0008;points.rotation.y+=.00015;group.rotation.y+=(mx*.25-group.rotation.y)*.018;group.rotation.x+=(-my*.16-group.rotation.x)*.018;renderer.render(scene,camera)}render();gsap.from('.hero h1',{y:80,opacity:0,duration:1.25,ease:'power4.out'});gsap.from('.hero .eyebrow,.lead,.scrollcue',{opacity:0,y:18,duration:1,stagger:.12,delay:.45});gsap.to(group.position,{x:-1.65,y:.1,scrollTrigger:{trigger:'#system',start:'top bottom',end:'center center',scrub:1.4}});gsap.to(group.scale,{x:.7,y:.7,z:.7,scrollTrigger:{trigger:'#system',start:'top bottom',end:'center center',scrub:1.4}});gsap.from('.chapter-copy',{x:80,opacity:0,scrollTrigger:{trigger:'#system',start:'top 65%',end:'center 55%',scrub:1}});gsap.to(group.position,{x:1.7,y:-.2,scrollTrigger:{trigger:'#signal',start:'top bottom',end:'center center',scrub:1.3}});gsap.to(group.scale,{x:1.25,y:1.25,z:1.25,scrollTrigger:{trigger:'#signal',start:'top bottom',end:'center center',scrub:1.3}});gsap.from('.statement h2',{opacity:0,y:70,scrollTrigger:{trigger:'#signal',start:'top 65%',end:'center 55%',scrub:1}});document.querySelectorAll('[data-target]').forEach(el=>{const o={v:0};gsap.to(o,{v:+el.dataset.target,duration:1.8,scrollTrigger:{trigger:el,start:'top 90%'},onUpdate:()=>el.textContent=Math.round(o.v)+(el.dataset.target==98?'%':'')}})});gsap.to(group.position,{x:0,y:0,scrollTrigger:{trigger:'#contact',start:'top bottom',end:'center center',scrub:1}});gsap.to(group.scale,{x:2,y:2,z:2,scrollTrigger:{trigger:'#contact',start:'top bottom',end:'bottom bottom',scrub:1}});gsap.to(mat,{opacity:.07,scrollTrigger:{trigger:'#contact',start:'top 70%',end:'bottom bottom',scrub:1}});ScrollTrigger.create({start:0,end:'max',onUpdate:s=>document.querySelector('#progress').style.width=(s.progress*100)+'%'});addEventListener('resize',()=>{camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix();renderer.setSize(innerWidth,innerHeight)});if(matchMedia('(prefers-reduced-motion: reduce)').matches){gsap.globalTimeline.timeScale(100)}